# Environment Configuration
ENVIRONMENT=production
DEBUG=false

# Security
SECRET_KEY=your-super-secret-key-here-change-this

# Database (Railway will provide this automatically)
DATABASE_URL=sqlite:///./networking_app.db

# Frontend URL (Update this after deploying frontend)
FRONTEND_URL=https://your-frontend-domain.vercel.app

# CORS Origins (comma-separated list)
ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app,http://localhost:3000,http://localhost:3001,http://localhost:3002

# Optional: Email service (for user notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Optional: Payment processing (for subscriptions)
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Optional: Email marketing
SENDGRID_API_KEY=SG...